// Message error types for curp client
syntax = "proto3";

import "google/protobuf/empty.proto";

option go_package = ".;curpapi";

package errorpb;

enum ProposeError {
    KeyConflict = 0;
    Duplicated = 1;
}
